/* eslint-disable no-shadow, no-param-reassign */
import fiats from '@/static/fiats.json';

const state = {
  fiats,
};

const getters = {
};

const actions = {
};

const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations,
};
